{
    "version": "0.2.0",
    "configurations": [
        

      {
        "type": "python",
        "request": "launch",
        "name": "Launch my_intent_extractor",
        "program": "${workspaceFolder}/my_intent_extractor.py",
        "console": "integratedTerminal",
        "cwd": "${workspaceFolder}"
      }
    ]
  }